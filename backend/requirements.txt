fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.12,<1.0
httpx>=0.27,<1.0

# Document parsing
pypdf>=5.0,<6.0
# Layout-aware PDF pass: recovers bordered tables (cell coordinates,
# rows/columns) that pypdf's flat text loses. Pure-Python wheels
# (pdfminer.six, Pillow, pypdfium2) — no system packages, no Java,
# no Ghostscript, so the Docker/AWS image is unchanged.
pdfplumber>=0.11,<0.12
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0

# Knowledge Base Indexing
openai>=1.50,<2.0
chromadb>=0.5,<1.0
tiktoken>=0.7,<1.0

# Knowledge Assistant Retrieval: local (no OpenAI call) lexical/BM25
# retrieval, one leg of hybrid retrieval alongside vector search. Pure
# Python, ~9KB, only depends on numpy (already pulled in by chromadb) —
# no model download, no new heavy dependency.
rank-bm25>=0.2,<0.3

# Dev/test
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
pytest-cov>=5.0,<6.0
reportlab>=4.0,<5.0
ruff>=0.7,<1.0
mypy>=1.13,<2.0
