/** Document types accepted by the Upload Documents dropzone. */
export const ACCEPTED_FILE_EXTENSIONS = [".pdf", ".docx", ".txt", ".csv", ".xlsx"];

export const ACCEPTED_FILE_ACCEPT_ATTR = ACCEPTED_FILE_EXTENSIONS.join(",");

export const ACCEPTED_FILE_HINT = "PDF, DOCX, TXT, CSV, or XLSX";
